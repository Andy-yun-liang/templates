services:
  postgres:
    image: postgres:16
    container_name: postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mydb
      TZ: America/Los_Angeles
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
